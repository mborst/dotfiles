# Keep terminal/window titles in sync with tmux context.
# Works with Ghostty (and other terminals that honor OSC title updates).
set -g set-titles on
set -g set-titles-string "#{session_name}:#{window_index}.#{pane_index} #{pane_title}"

# Keep pane titles explicit when needed:
# tmux select-pane -T "<title>"
